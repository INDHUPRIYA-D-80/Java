public class TernaryOperator {
    public static void main(String[] args) {
        int a = 1;
        int b = 2;
        int result;

        // Using the ternary operator to compare a and b
        result = (a < b) ? a : b; // This will assign a if a < b, otherwise b

        System.out.println(result);
    }
}
